
spring:
  h2:
    console:
      enabled: 'true'
  datasource:
    username: sap
    url: jdbc:h2:file:~/data/shipmentDB
    driverClassName: org.h2.Driver
    password: password
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    hibernate:
      ddl-auto: update
server:
  port: 9094